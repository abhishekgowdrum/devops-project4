- name: Deploy application
  hosts: webserver
  become: yes

  tasks:

    - name: Install Docker
      ansible.builtin.yum:
        name: docker
        state: present

    - name: Start Docker
      ansible.builtin.service:
        name: docker
        state: started
        enabled: yes

    - name: Pull Docker image
      community.docker.docker_image:
        name: YOUR_DOCKERHUB_USERNAME/YOUR_IMAGE:latest
        source: pull

    - name: Run Docker container
      community.docker.docker_container:
        name: myapp
        image: YOUR_DOCKERHUB_USERNAME/YOUR_IMAGE:latest
        state: started
        restart_policy: always
        ports:
          - "80:5000"