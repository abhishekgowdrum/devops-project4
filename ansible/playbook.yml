- name: Deploy application
  hosts: webserver
  become: yes

  tasks:

    - name: Install Docker
      ansible.builtin.yum:
        name: docker
        state: present

    - name: Start Docker
      ansible.builtin.service:
        name: docker
        state: started
        enabled: yes

    - name: Pull Docker image
      community.docker.docker_image:
        name: abhishekgowdrum/dockerlab1:commit_image
        source: pull

    - name: Run Docker container
      community.docker.docker_container:
        name: myapp
        image: abhishekgowdrum/dockerlab1:commit_image
        state: started
        restart_policy: always
        ports:
          - "80:8000"
